name: Release Core

on:
  push:
    tags:
      - "core-v*.*.*"

permissions:
  contents: write

jobs:
  release:
    if: contains(github.ref, "refs/tags/core-v")
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          clean: true

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"

      - run: npm ci

      - run: npm run build -w @fefade-ui/core

      - run: npm publish -w @fefade-ui/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
