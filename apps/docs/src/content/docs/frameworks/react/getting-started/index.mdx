---
title: Installation
description: How to install and set up fefade-ui in your project
head:
  - tag: title
    content: Installation in React
categories: ["react"]
---

import { Button } from "@fefade-ui/react"
import { Tabs, TabItem } from "@astrojs/starlight/components"
import { Steps } from "@astrojs/starlight/components"
import { FileTree } from "@astrojs/starlight/components"

<Steps>
    <ol>
        <li>
            ### Install
            <Tabs>
                <TabItem label="npm" icon="npm">
                    ```bash
                    npm install @fefade-ui/core @fefade-ui/react
                    ```
                </TabItem>
                <TabItem label="pnpm" icon="pnpm">
                    ```bash
                    pnpm add @fefade-ui/core @fefade-ui/react
                    ```
                </TabItem>
            </Tabs>
        </li>

        <li>
            ### Setup provider
            Wrap your application with the `Provider` to enable all fefade-ui components.
            This should be done at the layout level to ensure global access.

            > Recommended: Place this in `src/main.tsx` (React root element)

            <FileTree>
                - src
                    - main.tsx
            </FileTree>

            ```tsx
            import { StrictMode } from "react"
            import { createRoot } from "react-dom/client"
            import App from "./App"
            import { Provider } from "@fefade-ui/react"

            createRoot(document.getElementById("root")!).render(
                <StrictMode>
                    <Provider>
                        <App />
                    </Provider>
                </StrictMode>
            )
            ```
        </li>

        <li>
            ### Enjoy!
            <Tabs>
                <TabItem label="Code">
                    ```tsx
                    import { Button } from "@fefade-ui/react"

                    function App() {
                        return (
                            <>
                                <Button>
                                    Click me
                                </Button>
                            </>
                        )
                    }

                    export default App
                    ```
                </TabItem>
                <TabItem label="Preview">
                    <Button>
                        Click me
                    </Button>
                </TabItem>
            </Tabs>
        </li>
    </ol>

</Steps>
