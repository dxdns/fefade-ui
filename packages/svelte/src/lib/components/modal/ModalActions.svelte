<script lang="ts">
	import type { HTMLAttributes } from "svelte/elements"
	import { classMapUtil, mergeStyleUtil, type AlignType } from "@fefade-ui/core"
	import styles from "@fefade-ui/core/styles/ModalActions.module.css"

	interface Props extends HTMLAttributes<HTMLDivElement> {
		align?: AlignType
	}

	let {
		class: className = "",
		align = "end",
		children,
		...rest
	}: Props = $props()
</script>

<div
	{...rest}
	class={classMapUtil(className, [className, styles], styles.actions)}
	style={mergeStyleUtil(`--align: ${align};`, rest.style)}
>
	{@render children?.()}
</div>
